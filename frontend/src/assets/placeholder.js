// Create a placeholder.js file to export a function that generates placeholder image URLs
export default function placeholder(width, height) {
  return `https://via.placeholder.com/${width}x${height}`
}
